/**
 * 서비스 정책(권한·앨범·조직) — 구현 기준점.
 *
 * 1) 모드·기능: B2C/B2B 보호자 모두 모드(기능) 사용에 대한 소프트웨어 잠금 없음.
 *    비용·용량은 전자앨범 저장 등 리소스 쿼터로만 제한.
 * 2) 조직 생성: 슈퍼관리자(플랫폼 관리자)만. 일반 `createTenant`는 서버에서 차단됨.
 * 3) 한 사용자·여러 조직 태그: 허용 범위는 합집합(OR). 테넌트별 모드 화이트리스트로 기능을 막지 않음.
 * 4) 조직 관리자 대시보드: `tags.tenant_id` + 연결된 `pets.owner_id`로 “우리 태그를 쓰는 보호자”만 조회 (`tenant-tag-customers`).
 *
 * 발견자(NFC 공개) 화면: 별도 변경 요청 전까지 `PetProfileClient`·`nfc-public-display` 등 **현재 구현 유지**.
 * 개인정보·동의 문구는 운영 정책과 별도로 관리한다.
 *
 * 스토리지: DB는 MB 단위 프로필을 사용하며, 운영 상 “바이트 기준” 제한과 동일하게 취급 가능.
 */
